import React from "react";
import {} from "@/api";

export default () => {
  const handleInit = async () => {
    try {
    } catch (error) {
      console.log(error);
    }
  };

  return (
    <div>
      <div>编辑歌单信息</div>
    </div>
  );
};
